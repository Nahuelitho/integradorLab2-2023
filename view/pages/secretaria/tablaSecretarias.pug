extends ../../layout/plantilla
block contenido
    link(rel="stylesheet", href="../css/TablaPacientes.css")
    div(class="container", id="dttablaSecretarias") 
      h3(style="text-align: center", id="titulo") Secretarias
      div(class="row")
        div(class="col")
            if personas.length  !== 0
              table(id="tablaSecretarias", class="table table-striped")
                thead
                    tr
                      th(class="centered") Nombre
                      th(class="centered") Apellido
                      th(class="centered") DNI 
                      th(class="centered") Email
                      th(class="centered") Titulo
                tbody
                  each persona in personas 
                  
                    tr
                      td(class="centered")= `${persona.nombre}`  
                      td(class="centered")= `${persona.apellido}`  
                      td(class="centered")= `${persona.dni}`  
                      td(class="centered")= `${persona.email}`  
                      td(class="centered")= `${secretaria.titulo}`
            else
              h2(style="text-align: center") No se encontraron secretarias cargadas.
            button.btn.btn-primary(disabled=true, id="agregarPaciente", data-toggle='modal', data-target='#myModal') Abrir Formulario
            //- este boton abre un formulario que esta en otro archivo.
            //-este es el formulario-//
            include formPrueba.pug
             
           
    
        //- button(onclick="ocultar('ocultable')") ocultar tabla
        //- Más filas aquí
    script(src="https://code.jquery.com/jquery-3.7.0.js")
    script(src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js")
    script(src="https://cdn.datatables.net/select/1.7.0/js/dataTables.select.min.js")
    script(src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js")
    script(src="../js/iniciarTablaPacientes.js")